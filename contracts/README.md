# contracts/generated

Generated API contract artifacts (`openapi.json`, consumed by
`frontend/lib/api/schema.ts`). Produced by `make contracts` — never
hand-edited. Commit them so frontend builds do not require a running
backend.
